================================================================
⚡ URGENT FIX - "column orders.order_number does not exist"
================================================================

PROBLEM FOUND:
Two tables named "orders" in the same database location!
  • orders (analytics) - has order_id
  • public.orders (Stripe) - has order_number
They CONFLICT! Dashboard queries wrong table.

================================================================
SOLUTION (1 minute):
================================================================

Step 1: Go to Supabase Dashboard
→ https://app.supabase.com
→ SQL Editor

Step 2: Run this file:
→ analytics-backend/supabase/FIX_ORDERS_TABLE_CONFLICT.sql

Step 3: Restart your dashboard
→ Stop server (Ctrl+C)
→ npm run dev
→ Refresh browser

DONE! Error should be gone. ✅

================================================================
WHAT IT DOES:
================================================================

Renames: public.orders → stripe_orders
Result: No more table conflict!

After fix:
  • orders (analytics) - has order_id ✅
  • stripe_orders - has order_number ✅

================================================================
CODE ALREADY UPDATED:
================================================================

I've already updated your code:
  ✅ src/utils/supabase/orders.ts
  ✅ src/services/supabaseIntegration.ts
  ✅ src/components/DashboardShopify.tsx

Just run the SQL fix and restart!

================================================================
VERIFICATION:
================================================================

In Supabase SQL Editor, run:
  SELECT * FROM stripe_orders LIMIT 1;

Should return data (not error).

In Dashboard:
  • Open browser console (F12)
  • Look for "Stripe Orders: X rows from stripe_orders table"
  • Error should be GONE!

================================================================
NEED MORE HELP?
================================================================

Read: 🔴_CRITICAL_FIX_ORDERS_TABLE.md

================================================================

